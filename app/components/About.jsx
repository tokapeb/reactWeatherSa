var React = require('react');

var About = (props) => {
  return (
    <div>
      <h1>About</h1>
      <p>This is a weather application build in React. The only purpose is learning.</p>
    </div>
  );
}

module.exports = About;
